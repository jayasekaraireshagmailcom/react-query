import React from 'react';
const Navbar = () =>{
     return (
          <nav>
               <button>Planet</button>
               <button>People</button>
          </nav>
     )
}

export default Navbar;